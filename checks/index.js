/**
 * You almost certainly don't actually want to mount the template check.
 * However, this demonstrates the expected patter
 */
const wikisJsonValid = require("./wikis-json-valid");

module.exports = {
  wikisJsonValid
};
